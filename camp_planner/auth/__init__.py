"""Authentication & authorization: the identity contract, permission rules, and
the per-mode providers (standalone / proxy / embedded callback).

Attaching these to a Flask app (blueprints + request/template hooks) lives in
camp_planner.integration.
"""
